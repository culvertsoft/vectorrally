apply plugin: "java"
apply plugin: "eclipse"

sourceCompatibility = 1.7
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/", "src_generated" ]

eclipse.project {
    name = appName + "-core"
}
